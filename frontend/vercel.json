{
  "version": 2,
  "buildCommand": "echo vercel.config && npm run build ",
  "installCommand": "echo vercel.config && npm install",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "${process.env.API_URL}/api/$1" }
  ]
}