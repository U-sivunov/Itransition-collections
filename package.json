{
  "name": "fullstack-vue-express-app",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "set NODE_OPTIONS=\"--max-old-space-size=460\" && concurrently \"cd frontend && npm install && npm run serve\" \"cd backend  && npm install && npm start\"",
    "build": "concurrently \"npm install\" \"cd frontend && npm install && npm run build\" \"cd backend && npm install\"",
    "deploy": "cd backend && npm install && npm start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {}
}